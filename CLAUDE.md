# CARD ARCADE

日本のトランプゲームを参加者9名で手分けして作り、Pull Request で1つのアーケードに統合する研修用リポジトリです。
1人が1ゲームを、調査から実装・テスト・Pull Request の提出まで最後まで担当します。

## 絶対に守る5条

1. **編集してよいのは `src/games/<自分のゲームID>/` の中だけ。** 例外はありません。
2. **依存を追加しない。** `npm install <パッケージ>` は使いません。必要なものは `@core` と `@ui` に揃っています。
3. **他の人のゲームフォルダを変更しない。**
4. **`src/core/` `src/components/` は運営管理。** 読むのは自由ですが、変更はできません。
5. **`npm run verify` が成功するまで「できました」と言わない。**

範囲外を編集しようとするとフックが止め、Pull Request では CI が落ちてマージできません。
迂回策（`sed -i`、リダイレクト、`cp` での上書きなど）を探さないでください。
共通基盤の変更が必要だと判断した場合は、**手を止めて講師に相談する**のが正しい行動です。

## 自分の担当を知る

作業ブランチの名前が担当を表します。

```
feature/<ゲームID>     例: feature/babanuki
```

| ゲームID | ゲーム | 担当 | 難易度 | Issue |
|---|---|---|---|---|
| `babanuki` | ババ抜き | 担当1 | 初級 | #1 |
| `daifugo` | 大富豪 | 担当2 | 上級 | #6 |
| `shinkeisuijaku` | 神経衰弱 | 担当3 | 初級 | #2 |
| `poker` | ポーカー | 担当4 | 上級 | #8 |
| `butanoshippo` | ぶたのしっぽ | 担当5 | 初級 | #9 |
| `speed` | スピード | 担当6 | 中級 | #3 |
| `shichinarabe` | 七並べ | 担当7 | 中級 | #4 |
| `doubt` | ダウト | 担当8 | 中級 | #5 |
| `pageone` | ページワン | 担当9 | 中級 | #10 |

担当・ゲームID・Issue 番号の正本は `harness/config.json` です。
`main` ブランチのまま実装を始めてはいけません。先に `git switch -c feature/<ゲームID>` を実行してください。

## ターミナルは2本

| | ターミナルA | ターミナルB |
|---|---|---|
| 中身 | Claude Code のセッション | 人間が自分の手だけで打つ PowerShell |
| 常駐 | — | `npm run dev`（研修中つけっぱなし） |
| やること | 調査・実装の依頼・差分の確認・文章の下書き | 実機で遊ぶ / `npm test` の自分確認 / `gh pr ready` |

`npm run dev` と監視モードのテスト（`npm run test:watch` など）は、**ターミナルA では実行できません。**
起動したままになってセッションが返らず、ターミナルB の 5173 番とポートが衝突するためです。
ハーネスが拒否メッセージを出します。回り込む方法を探さないでください。

## コマンド（覚えるのはこの3つ）

```
npm run dev      開発サーバーを起動する（ターミナルB で1回だけ。あとは付けっぱなし）
npm test         テストを実行する
npm run verify   提出前の全部入りチェック（CI とまったく同じ内容）
```

`npm run verify` は 範囲チェック → lint → 型チェック → テスト → ビルド を順に実行します。
**これが成功して初めて「できた」と言えます。**

コードを変えたあと画面に反映するのは、ターミナルB のブラウザ（http://localhost:5173/）の**再読み込み**です。
開発サーバーを起動し直す必要はありません。

## ゲームフォルダの構成（5ファイル固定）

```
src/games/<ゲームID>/
├─ index.ts            ゲームの公開情報（GameManifest）。完成したら status を "ready" にする
├─ <Xxx>Game.tsx       画面。見た目と「時間」だけを担当する
├─ logic.ts            ルール。純粋関数だけを書く
├─ logic.test.ts       ロジックのテスト。ここが評価対象
└─ README.md           遊び方 / ルール / 実装メモ
```

必要なら `cpu.ts`（CPU の判断）、`types.ts`、`<Xxx>Game.module.css` を足してかまいません。

## いちばん大事な型

待ち時間のあるゲーム（CPU の手番、カードを裏返す演出）は、**必ず**この形にします。

```ts
// logic.ts — 時間の概念を持たない
export function reduce(state: S, action: A): S;
export function pendingDelayMs(state: S): number | null;  // 今、何ms後に自動処理が要るか
```

```tsx
// <Xxx>Game.tsx — 時間だけを担当する
useCpuTurn(pendingDelayMs(state), () => dispatch({ type: "tick" }));
```

こうするとテストは `reduce` を順番に呼ぶだけになり、`setTimeout` を一切テストしなくて済みます。
お手本は `src/games/example-game/` にあります。**まずそこを読んでください。**

## コード規約

- 共通機能は `import { ... } from "@core"` と `import { ... } from "@ui"` だけで読み込む
  （`@core/deck` のような深い指定はできません）
- `logic.ts` と `cpu.ts` では `react` を import しない・`Math.random()` `Date.now()` `setTimeout` を使わない
  （乱数は引数で `Rng` を受け取り、テストでは `createRng(seed)` を渡します）
- 保存は `@core` の `useHighScore` / `gameKey` を使う（`localStorage` の直接利用は禁止）
- スタイルは CSS Modules。色や余白は `src/styles/tokens.css` の `--ca-*` を使い、直書きしない
- CPU は単純なルールベースで十分。凝った探索は作らない

## 日本語の文章ルール

Markdown、README、Pull Request、Issue、画面上の文言を書くときは、次のルールに従います。
英語を逐語訳したような文章や、AI が作ったと感じさせる大げさで抽象的な文章は使いません。

### 自然な日本語で書く

- 英文を語順どおりに訳さず、日本語として自然な主語・語順・接続に組み直す。
- 短文を「〜します。〜します。〜します。」と並べず、関係の深い内容は一つの文や段落にまとめる。
- 「〜することです」「〜のためです」「これが〜です」を繰り返さない。理由が必要な場合は、行動と理由を自然につなぐ。
- 「〜させます」「〜を出します」「〜を見ます」のような不自然な直訳表現を避け、誰が何をするのかを具体的に書く。
  - 悪い例: `Claude Code に計画を出させます。`
  - 良い例: `Claude Code に実装計画の作成を依頼します。`
  - 悪い例: `状態を画面に出します。`
  - 良い例: `ゲームの状態を画面に反映します。`
- 文脈から明らかな主語は省く。「あなた」「人間」「自分の手で」「自分の目で」を何度も繰り返さない。
- Claude を指す名称は `Claude Code` に統一する。参加者との対比が必要な場合は「人間」ではなく「参加者」と書く。

### 意味を具体的にする

- 抽象的な決めぜりふや、説明になっていない比喩を作らない。
  `丸投げ`、`時間を溶かす`、`検問`、`儀式`、`リング`、`逃げ道`、`次の一手を出す` などは、具体的な行動に言い換える。
- `いい感じに`、`しっかり`、`適切に`、`必要に応じて` のような曖昧な言葉だけで指示を終えない。対象、操作、完了条件を明記する。
- 根拠のない断定や心理の決めつけをしない。
  `必ず失敗する`、`読んでいない証拠です`、`誰も気づきません` のように書かず、起こり得る問題と対処を説明する。
- 強い命令や不安をあおる表現は、危険な操作の禁止や必須要件に限る。それ以外は、落ち着いた案内文にする。
- `赤い` / `緑`、`Draft を外す`、`ブランチを切る` などの口語は、初学者向けの説明では `失敗` / `成功`、`Draft 状態を解除する`、`ブランチを作成する` と書く。

### 読みやすく整える

- 一つの段落では一つの話題だけを扱う。結論または実行する操作を先に書き、理由や補足をあとに続ける。
- 同じ内容を本文、引用、箇条書きで何度も繰り返さない。強調のためだけの言い換えも加えない。
- 太字を多用しない。本当に見落としてはいけない条件、禁止事項、操作対象だけを強調する。
- 見出しは内容を具体的に表す。キャッチコピーのような見出しや、本文を読まないと意味が分からない見出しは避ける。
- 手順では `入力する`、コマンドでは `実行する`、結果では `成功する` / `失敗する` を使い分ける。
- 英数字、コマンド、パス、プレースホルダーは改変せず、バッククォートで囲む。製品名や用語の表記を文書内で統一する。

### 書き終えたら確認する

文章を作成または修正したあとは、次を確認してから完了と報告します。

1. 声に出して読んでも、不自然な語順や同じ語尾の連続がないか。
2. 比喩や抽象語を使わず、読者が次に行う操作を理解できるか。
3. 同じ意味の説明や過剰な強調を削れるか。
4. コマンド、パス、プレースホルダー、引用文を誤って変更していないか。

既存のエラーメッセージ、UI 上の固定文言、ユーザーが「そのまま使う」と指定した文章は、引用部分を変更しません。

## テストの書き方

テストは、`logic.ts` に関数を足すのと同時に書きます。あとでまとめて書こうとしないでください。
`status` を `"ready"` にするゲームには、`logic.test.ts` に `it(` が**3件以上**必要です
（件数は `harness/config.json` の `minTestsPerReadyGame`。`it.skip` は数えません）。

まず正常系を書き、余裕があれば次のような**壊れやすいところ**も足します。

- 境界値（同じ数字、最後の1枚、0枚）
- やってはいけない操作が弾かれること（判定中の二重クリック、出せないカードを出す）
- seed を固定した決定的なテスト

## 迷ったときの判断

- **仕様が曖昧** → Issue に書かれているルールが正です。Issue に無いことは「実装しない」を選びます。
- **共通基盤に手を入れたくなった** → 手を止めて講師に相談します。自分で直しません。
- **時間が足りない** → 発展課題を捨てて、Issue の必須要件だけに絞ります。
- **エラーの直し方が分からない** → `docs/troubleshooting.md` を見てから相談します。
- **手が止まった** → ブランチ、`npm run scope`、`npm test` の結果を集めて状況を整理し、
  そのまま講師に見せます。1人で作っているので、黙って詰まっていると誰も気づけません。
  相談は遅れではなく手順です。

## 実装を進める順番

作業は、Claude Code への日本語のプロンプトで進めます。覚えるコマンドはありません。

1. Issue と `docs/games/<ゲームID>.md` を原文で読む（要約させません）
2. 実装の計画を立ててもらい、人間が Issue の必須要件と照合して合意する
3. **まず最後まで遊べる形まで実装する。** 発展課題には手を付けない
4. 人間がブラウザで遊び、気づいたことを伝える。プランモードで改善策を計画させ、
   内容を確認してから実行させる。これを繰り返す
5. 残りの必須要件を足し、`index.ts` の `status` を `"ready"` にする
6. `npm run verify` を成功させ、Pull Request の本文を `.pr-body.md` に下書きする
   （push と `gh pr ready` は人間がターミナルB で打ちます）

マージは講師が行います。参加者はマージしません。

手を止めるときや講師に状況を渡すときは、いまの状況を10行以内でまとめてもらいます。
推測で書かせず、`git status` などで確認した事実にもとづかせてください。

進み具合は Issue のチェックリストで表します。作業を1つ終えるたびにチェックを入れてください。
1人開発ではそれが唯一の外から見える進捗です。

## 人間（参加者）が必ず自分で確認すること

AI の出力をそのまま信じないでください。次の4点は人間が確認します。

1. **実装の計画** が Issue の要件と合っているか（実装を任せる前に確認します）
2. **実機で遊べるか**（ターミナルB のブラウザを再読み込みして、最初から最後まで1回プレイする）
3. **直した結果が画面に反映されているか**（テストが成功していても、画面に問題が残ることがあります）
4. **Pull Request の説明** が、自分の言葉で書かれているか

**Claude Code へ:** `npm run dev` と監視モードのテストは実行しない。画面の確認は人間に依頼する。
